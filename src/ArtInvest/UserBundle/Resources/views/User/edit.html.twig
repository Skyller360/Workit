{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User edit</h1>
    
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('_index') }}">
            Back to the index
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
<label>Change Password</label>
{{ form(password_form) }}

{% endblock %}
